{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "fullstack course ex 3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../phonebook_frontend/PB_frontend && npm run build && cp -r dist ../../phonebook_app",
    "deploy:full": "npm run build:ui && git add . && git commit -m UIbuild && git push origin main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
